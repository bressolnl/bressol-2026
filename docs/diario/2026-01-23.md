Día 14 — Metadatos de packs + recomendaciones PDP dinámicas (por focus/themes)

Qué se ha hecho:
- Ajustado el JSON del Pack Cocina para el esquema real del plugin (slots/options/required/min/max/surcharge)
- Ampliado el metabox de packs para incluir:
  - _bressol_pack_themes (CSV)
  - _bressol_pack_focus (CSV)
  - (y mantener tier/occasion)
- Configurado Pack Cocina Test (ID 88) con:
  - focus: oil,salt,vinegar
  - themes: cooking
  - tier: mid
  - occasion: both
- Refactor de recomendaciones en PDP:
  - Eliminadas dependencias hardcoded por IDs
  - Upsell pack se selecciona por _bressol_pack_focus / _bressol_pack_themes
  - Cross-sells se obtienen por categorías (salt/vinegar y olives/tapenade)
- Tracking:
  - bressol_reco_view sigue funcionando
  - bressol_reco_click sigue siendo robusto (query params + capture server-side + sesión + wp_head)
- Ocultados bloques nativos de Woo (related/upsells) durante el MVP para evitar confusión con el sistema propio

Qué he aprendido:
- La UI de packs depende de un esquema JSON específico; si no coincide, los selects quedan vacíos
- En tracking con navegación, el patrón robusto es capturar server-side y empujar desde sesión en el siguiente render
- Para recomendaciones, usar metadatos (focus/themes) permite escalar sin hardcode

Dudas / bloqueos:
- Ninguna

Pendiente para la próxima sesión:
- Implementar recomendaciones en carrito (alto impacto AOV)
- Añadir tracking específico de carrito:
  - bressol_cart_reco_view
  - bressol_cart_reco_click (mismo patrón robusto)
- Mejorar lógica para productos sin categoría usando fallback por metadatos (si aplica)